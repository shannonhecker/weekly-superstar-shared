// Spacing tokens. We use Tailwind's default 4pt scale everywhere; this file
// is a placeholder kept for future custom spacing (e.g. platform safe-areas)
// so the tokens barrel has a symmetric shape.

export const spacing = {} as const

export type Spacing = typeof spacing
